<div class="container teams_container">
  <div class="row mb-3">
    <div class="col-md-8"><h3 class="inner_heading rules_heading">Rules</h3></div>

    <div class="col-md-4">
      <div class="text-center game_time">

        <a href="javascript:void(0);" class="time_changer" data-type="plus"><%= image_tag 'minus.png' %></a>
        <span class="game_duration">30 </span> minutes
        <a href="javascript:void(0);" class="time_changer" data-type="minus">
          <%= image_tag 'plus.png' %></a>
      </div>
    </div>
  </div><!--/row-->

  <!--<h4 class="text-center">Rules</h4>-->
  <ul class="how_works_text">
    <li>The entire class is on the same team.</li>
    <li>Winning means solving every puzzle.</li>
    <li>Use screams when you're stuck.</li>
    <li>Search the internet for answer.</li>
    <li>If your device won't connect, share.</li>
    <li>When your clan finishes, go help other teams.</li>
  </ul>


  <div class="text-right">
  <span class="outer_btn text-center primery-gradient-l-r">
    <%= link_to 'Start Game', start_game_path(@game), class: 'start_game' %>
  </span>
  </div>


</div><!--container-fluid-->

<script>
    var timeSpan = <%= Game::DURATION %>;
    var action = $('a.start_game').attr('href');
    $('a.time_changer').on('click', function () {

        if ($(this).data('type') == 'plus') {
            if (timeSpan > 0) {
                timeSpan -= 1;
            }
        } else {
            if (timeSpan < 30) {
                timeSpan += 1;
            }

        }
        $('span.game_duration').html(timeSpan);
        $('a.start_game').attr('href', action + '?duration=' + timeSpan)
    });
</script>