<nav class="navbar navbar-light navbar-expand-md justify-content-between">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-nav"
          aria-controls="navbarSupportedContent"
          aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>

  <div class="collapse navbar-collapse justify-content-between dual-nav" id="navbarSupportedContent">
    <ul class="navbar-nav mr-0">
      <li class="nav-item active">
        <%= link_to 'javascript:void(0);', class: 'nav-link home' do %>
            <%= image_tag 'escape_monster.png', class: 'logo' %>
        <% end %>
      </li>
      <li class="nav-item mt-2">
        <%= link_to 'Help', games_introduction_path, class: 'nav-link' %>
      </li>
    </ul>
  </div>

  <div class="secret_knock">
    <h2>Secret Knock</h2>

    <div class="knock_table_div">
      <table class="table rounded">
        <tbody>
        <tr>
          <% @game.secret_knock.digits.insert(3, '-').reverse.each do |digit| %>
              <td><%= digit %></td>
          <% end %>
        </tr>

        </tbody>
      </table>

    </div>

  </div>
  <!--/secret_knock-->


  <div class="navbar-collapse collapse dual-nav">
    <ul class="nav navbar-nav ml-auto">
      <% if teacher_signed_in? %>
          <li class="dropdown">
            <a href='javascript:void(0);' , class="dropdown-toggle user_pic_name" id="dropdownMenuButton" data-toggle="dropdown">
              <span class="user_pic"><%= image_tag 'user_pic.png' %></span>
              <%= current_teacher&.name&.titleize %>
            </a>
            <div class="dropdown-menu header_logout_dropdown" aria-labelledby="dropdownMenuButton">
              <%= link_to destroy_teacher_session_path, method: :delete, class: "dropdown-item" do %>
                  <i class="fa fa-power-off" aria-hidden="true"></i> Logout
              <% end %>
            </div>

          </li>
      <% else %>
          <li class="nav-item">
        <span class="nav-link signin_btn">
          <%= link_to 'Sign in', new_teacher_session_path %> / <%= link_to 'Sign up', new_teacher_registration_path %>
        </span>
          </li>
      <% end %>
    </ul>
  </div>
</nav>


</div>
<!--container-fluid-->